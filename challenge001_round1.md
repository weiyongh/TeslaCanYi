# TeslaCanYi Challenge 001 — 第一轮独立探索

## 结论摘要

本次采集在 CAN 上呈现出完整且可逆的五阶段轨迹：未连接 → 已连接未充电 → 充电建立并保持 → 已停止仍连接 → 已断开。最强的单帧候选是 CAN ID `0x21D`；其多个 byte 在五阶段中稳定呈现不同组合，并在拔枪后回到初始组合。`0x43D` 提供了时间高度一致的第二条证据链。多组其他 ID 在充电建立和退出期间同步阶跃或连续爬升/衰减。

这些事实足以支持“存在连接、建立、保持、退出、断开五种可区分的总线状态”。但在禁止引入外部定义的条件下，不能把任何字段命名为实际电压、电流、功率、接触器或 Pack 电流；本文只给出无语义的 Signal Candidate 及其观测关系。

## 数据与时间轴

- 两段 ASC 共 1,219,531 帧、282 个 CAN ID，拼接后覆盖约 `0.000–539.911 s`。
- 第一段止于 `442.3720 s`，第二段从自身 `0.0000 s` 开始。分析按“第一段末尾 + 0.1 ms”拼接；第二段文件头墙钟为 13:44:50，与这一位置一致。第二段绝对时间因此存在不到约 1 s 的文件头精度不确定性，但不影响 E09/E10 的相对顺序。
- 操作提示点来自 `event_timeline.csv`，不被当作物理变点。桩端照片记录充电开始 `13:40:21`、结束 `13:43:51`；相对采集约为 `173 s` 和 `383 s`。
- 现场显示：开始后约 21 s 时为 `225.0 V / 3.66 kW`；约 28 s 时为 `224.9 V / 16.1 A / 3.62 kW`；约 58 s 时为 `222.1 V / 32.1 A / 7.13 kW`；约 2 分钟时为 `222.5 V / 32.3 A / 7.19 kW`。车端照片显示 `219–221 V / 32 A / 7 kW`。订单最终记录 0.38 kWh。

## 关键物理变点（CAN 自身发现）

| 相对时间 | 观察到的 CAN 变化 | 与现场事件的关系 |
|---:|---|---|
| `40.745 s` | `0x43D` 从 `00 00 ff 1f 40 00` 附近进入 `00 00 ff 1f 40 60` | E02 为 40.041 s；候选为开小门相关变化，但可能混入人员/车机活动 |
| `59.546–60.047 s` | `0x21D`、`0x43D` 同步进入新稳定组合 | 与 E03 60.037 s 标记重合（最早变化早约 0.49 s），可信对应插枪/连接建立 |
| `173.353–173.653 s` | `0x21D`、`0x43D` 多级切换；`0x204/0x212/0x252/0x264` 开始爬升 | 桩端订单开始时刻约 173 s；可信对应充电建立过程 |
| `175.434–175.793 s` | 多个候选到达稳定充电组合；`0x24A` 变为 `01` | 与桩端功率上升过程一致；约 2.4 s 建立完成 |
| `381.567–384.131 s` | 同一组 ID 反向退出；连续量衰减到零样式 | E08 为 380.102 s，订单结束约 383 s；可信对应正常停止过程 |
| `439.983–440.958 s` | `0x333` byte 3 的 `0x80` 短脉冲 | 与 E09 440.055 s 几乎重合；仅可称“解锁操作相关候选” |
| `470.174–470.574 s` | `0x21D` 回到初始组合；`0x43D`、`0x49D` 等同步回退 | 紧随 E10 470.103 s；可信对应拔枪/断开 |

## Signal Candidate

### A. `0x21D`：五阶段复合状态候选（最高置信）

在保守阶段窗口内，每个下列 modal value 的占比均为 100%。byte 从 0 开始编号。

| 阶段 | 完整稳定 payload | byte 0 | byte 2 | byte 4 | byte 6 |
|---|---|---:|---:|---:|---:|
| 初始未连接 | `04 00 35 00 00 00 00 00` | `04` | `35` | `00` | `00` |
| 已连接未充 | `4c 00 02 20 00 00 10 00` | `4c` | `02` | `00` | `10` |
| 稳态充电 | `2f 40 22 20 80 00 30 00` | `2f` | `22` | `80` | `30` |
| 停止仍连接 | `4c 00 02 20 80 00 10 00` | `4c` | `02` | `80` | `10` |
| 拔枪后 | `04 00 35 00 00 00 00 00` | `04` | `35` | `00` | `00` |

诊断价值：byte 0/2/6 联合可无语义地区分五阶段中的四种功能状态；byte 4 又把“开始前已连接”和“停止后仍连接”分开。整个 540 s 内完整 payload 只有 12 次变化，明显不像滚动计数器。

谨慎解释：`byte 6` 的 `00→10→30→10→00` 很像分层状态编码，但“连接/充电”只是基于实验事件的关联标签，不是已知定义。byte 4 的 `00→80` 在充电建立后一直保留到拔枪，可能是会话/锁止/历史状态中的任一种，不能单独命名。

### B. `0x43D`：独立的阶段确认候选（高置信）

- 插枪附近：`00 00 ff 1f 40 60` → `01 00 ff 1f 20 20` → `01 00 ff 1f 40 20`。
- 建立附近：`01 40 ff 1f 40 20` → `22 40 ff 1f 40 22` → `25 40 ff 1f 40 22`。
- 停止附近：`2d 00 ... 22` → `21 00 ... 22` → `21 00 ff 1f 20 26`。
- 拔枪后回到 `00 00 ff 1f 40 60`，随后 byte 5 到 `00`。

它与 `0x21D` 的主要变点相差通常只有数毫秒，因此适合作为交叉确认，而不应把两个 ID 当作两次独立实验重复。

### C. 建立/退出连续量候选（中高置信关联，语义未知）

- `0x264 byte[0:2]`（小端观察）：充电建立时从约 `0x000037` 快速升到约 `0x1Bxx`，稳态在 `0x19xx–0x1Axx` 一带，停止时反向下降。外部照片为约 7.1 kW 时，该原始量约为 6,650–6,700。它具有“功率样连续量”的形状，但比例和含义未经第二工况验证。
- `0x264 byte[2:4]` 在稳态约为 `0x46D0–0x48D0`，与 byte[0:2] 同时进入/退出；只可视为另一连续量候选。
- `0x204` 从 `02 00 00 ff ...` 经多级过渡进入 `66 02 01 xx ...`；停止时回到 `41 00 00 00 ...`，随后另一个状态 bit 清零。
- `0x212` 在建立时变为 `59 .. .. 40 .. .. 30 43` 类组合，停止时 byte 2 以阶梯形式从 `0x1D` 衰减至 `0x00`，其余状态 byte 回退。
- `0x252` 和 `0x2D2` 同步呈现建立/退出阶跃；`0x24A` 在 `175.793 s` 变为 `01`，在 `382.798 s` 变回 `03`，是低频离散状态候选。

这些候选的联合时序能证明“CAN 上存在与外部供电建立/退出同步的内部过程”，但不能仅凭波形证明它们分别属于交流输入、车辆侧直流输出或 Pack 结果。

### D. 连接/断开与操作候选

- `0x2EC byte 0`：阶段模式为 `2f,2f,67,67,67,67,2f`，即插枪后保持到拔枪；但该短帧的其他 byte 明显含滚动计数/校验样变化，应只取 byte 0。
- `0x2E8 byte 5` 与 `0x2A8 byte 5`：`00→80` 在连接后保持至拔枪，再回 `00`；同样需避开帧内计数器 byte。
- `0x333 byte 3 bit 7`：仅在 E09 附近产生约 0.975 s 脉冲，是解锁操作相关候选。单次操作且可能是 UI/请求而非执行反馈，置信度仅中等。

## Evidence Requirement 映射

| 要求 | 第一轮结论 |
|---|---|
| ER-01 连接识别 | 强支持：`0x21D/0x43D/0x2EC` 在插枪后同步进入稳定状态，拔枪后可逆恢复 |
| ER-02 准备/许可推进 | 支持“已连接未充”是可区分状态；不能把某一 bit 定义为许可 |
| ER-03 实际交流供电 | 外部桩端照片直接显示电压、电流、功率；CAN 建立变点与其时间一致 |
| ER-04 功率转换 | 有多个连续量候选同步爬升/衰减，但没有足够证据给它们命名为车辆侧直流输出 |
| ER-05 Pack 充电结果 | 车端显示充电、SOC 从 85% 到 86%、订单 0.38 kWh；CAN 中有结果样候选，但无法证明具体字段就是 Pack 结果 |
| ER-06 正常保持 | 强支持：约 175.8–381.6 s 保持稳定充电状态，且外部照片跨约 2 分钟保持 7.1–7.2 kW |
| ER-07 正常退出 | 强支持：停止过程与断开过程在时间上分离，`0x21D` 可区分停止仍连接和最终断开 |

## 原始 Evidence 回指

- 操作提示：`input/.../event_timeline.csv` 第 2–12 行。
- 照片元数据：`input/.../session.json` 第 133–218 行；照片内容来自相同 acquisition 目录的九张 JPG。
- `0x21D` 插枪变点：第一段 ASC 第 134011、134232 行；建立变点：第 390437、390657、390884、391116 行；停止变点：第 863138、864476、864712 行；拔枪变点：第二段 ASC 第 62529、63217、63438 行。
- `0x43D` 小门/插枪/建立/停止：第一段 ASC 第 91702、134012、135134、390453、390666、390902、863143、864479、864713 行；拔枪：第二段 ASC 第 63224、74253 行。
- `0x204/0x212/0x24A/0x49D` 的代表性建立和退出帧：第一段 ASC 第 390668、390824、390837、395154、395367、395960、863258、863283、864372、865057、865891、868219、868887 行。

## 没有发现或不能证明的内容

1. 未能从本次单一工况唯一确定任何候选的工程语义、单位、比例、端序或发送 ECU。
2. 未能把 ER-04/ER-05 分解为“交流侧量—直流侧量—Pack 结果”三个已命名 CAN 信号；现有证据只能证明多组量联合变化。
3. 未发现可由本次数据单独验证的充电许可因果链；同步变化不等于控制因果。
4. 没有初始状态照片，也没有 E10 拔枪后车机照片；因此最终物理断开的非 CAN 证据弱于插枪、充电和停止阶段。
5. E02、E09 附近的人为操作可能同时唤醒界面或改变门锁状态，单次实验无法排除混杂。

## 方法限制与下一轮改进

本轮使用阶段窗口的 modal value、逐 byte 稳定度、变化率惩罚、起末可逆性，以及事件附近的精确变点复核。主要限制是：只有一次完整周期；稳态电压/电流变化很小；操作、人员进入驾驶舱和车机交互相互混杂；ASC 分卷文件头只有秒级墙钟精度。

下一轮将：

1. 对所有 bit field 做带滞回的变点聚类，把同一内部状态机的同步字段分组，减少重复候选。
2. 对 8/12/16/24/32-bit、大小端和有符号解释做无语义数值扫描，用单调性、建立/退出对称性和照片锚点拟合度排名，但仍不据此直接命名。
3. 做“事件置换/伪锚点”检验，估计 173 s、382 s、470 s 附近出现同等强度同步变化的偶然概率。
4. 将驾驶舱活动窗口单列为混杂变量，筛掉只在拍照/开门期间变化而不随充电周期可逆的字段。
5. 若未来允许新增采集，优先做重复插拔、不同电流阶跃、停止后延迟拔枪和不进入驾驶舱的对照；这些是区分连接、功率、锁止及 UI 状态的最小实验集。
