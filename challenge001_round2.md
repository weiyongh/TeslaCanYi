# TeslaCanYi Challenge 001 — 第二轮独立探索

## 结论摘要

第二轮仍只使用 `input/`。总比分没有被用于反推任何具体答案，只说明第一轮的候选集合中同时存在有效关联、定位偏粗和误命中。

第一轮的主要问题是字段粒度：若干候选以整 byte 或 message-level 提交，但重新查看原始变化后，可以确认其中混有多个独立 bit、连续量、周期计数或阶段子状态。另一些被第一轮拆成单 byte 的量，实际上更像跨 byte 连续量。第二轮因此不再保留任何 message-level 点位。

核心五阶段结论没有改变：CAN 仍清楚地区分未连接、已连接未充、充电、停止仍连接和断开。最稳定的生命周期骨架仍由 `0x21D` 与 `0x43D` 提供；第二轮提高了它们的 bit-field 精度。连续过程方面，`0x132`、`0x252`、`0x264` 的跨 byte 原始量比第一轮的单 byte 模式更有诊断价值。

## 第二轮对第一轮候选的处理

| 第一轮点位 | 第二轮处理 | 原因 |
|---|---|---|
| `0x21D byte 0` | 降级，不再按完整 byte 提交 | byte 内包含多种阶段变化，单一字段边界不足以确认 |
| `0x21D byte 2` | 降级，不再按完整 byte 提交 | 多个 bit 呈不同阶段模式，整 byte Claim 过宽 |
| `0x21D byte 4` | 精确为 byte 4 bit 7 | 只有 bit 7 构成充电建立后保持、拔枪后恢复的干净模式 |
| `0x21D byte 6` | 精确为 byte 6 bits 4:6 | 右移四位后依次呈现 `0→1→3→4→1→0` 的生命周期状态序列 |
| `0x43D message-level` | 拆分为多个 field-level 点位 | byte 0、1、5 内存在彼此可区分的连接、充电窗口和阶段子状态 |
| `0x264 byte 0:1` | 修正为 15-bit 小端观察量 | byte 1 bit 7 周期性翻转，不属于平滑连续量主体 |
| `0x264 byte 2:3` | 保留 | 建立后连续增长，停止时反向归零 |
| `0x204 message-level` | 删除，替换为 byte 7 bit 0 | 整帧含多个变化来源；bit 0 给出更干净的充电窗口 |
| `0x212 byte 2` | 保留 | 建立后增长、停止后阶梯衰减到零 |
| `0x212 message-level` | 删除，另保留 byte 3 bit 6 | message-level 边界过宽 |
| `0x252 message-level` | 拆为 byte 0:1 与 byte 2:3 | 两组连续量具有不同建立和退出轨迹 |
| `0x2D2 message-level` | 从最终命中表移除 | 确有周期关联，但字段边界和独立性仍不足 |
| `0x24A byte 0` | 精确为 byte 0 bit 1 | 全周期只有该 bit 构成充电窗口变化 |
| `0x2EC byte 0` | 保留 | 连接后保持、拔枪后恢复；帧内其他 byte 为周期变化 |
| `0x2E8 byte 5` | 精确为 bit 7 | bit 7 单独构成连接周期状态 |
| `0x2A8 byte 5` | 精确为 bit 7 | 同上 |
| `0x333 byte 3 bit 7` | 保留 | 解锁操作附近唯一短脉冲，但仍只主张“操作相关” |
| `0x49D byte 6` | 精确为 bit 5 | 充电建立后置位、停止后保持、拔枪后清除 |

## 关键精确候选

### 1. `0x21D`

- byte 3 bit 5：插枪后置位，穿过充电与停止阶段，拔枪后清除。
- byte 4 bit 7：充电建立时置位，停止后仍保持，拔枪后清除。
- byte 6 bits 4:6：阶段值依次为 `0、1、3、4、1、0`；其中 `4` 只出现在停止瞬态。该字段是第二轮保留的最强生命周期状态候选。

byte 0 和 byte 2 虽仍与阶段高度相关，但第二轮不再把它们作为完整 byte 字段提交。

### 2. `0x43D`

- byte 0 bit 0：插枪后置位，拔枪后清除。
- byte 0 bit 2：充电建立完成附近置位，停止开始附近清除。
- byte 1 bit 6：充电建立开始时置位，停止开始时清除。
- byte 5 bit 5：开小门后置位，跨越连接周期，拔枪后一段时间才清除。
- byte 5 bits 0:2：建立后为 `2`、停止仍连接时为 `6`、其他阶段为 `0`，可区分已建立与已停止仍连接。

其中 byte 0 bit 2 与 byte 1 bit 6 的边沿并不完全同时，说明它们值得作为两个点位对照，而不是一个重复 Claim。

### 3. `0x132` 连续量组

- byte 0:1 小端观察量：充电保持期间由约 `0x8Axx` 缓慢上升到 `0x8Bxx`，停止后缓慢回落。
- byte 2:3 有符号小端观察量：非充电时接近小负值，建立后由负值跨零并增长到约 `0x00B5`，停止时快速回到接近零的小负值。
- byte 4:5 小端观察量：与上述过程共同变化，但噪声更高，置信度低于前两项。

这三项只被称为“充电过程相关连续量”。第二轮不为其分配电压、电流、功率或温度语义。

### 4. `0x264` 连续量与状态

- byte 0 加 byte 1 bits 0:6 构成的 15-bit 小端观察量：约 `173.6 s` 开始快速上升到约 `0x1Bxx`；停止后平滑衰减到接近零。byte 1 bit 7 会独立翻转，因此不再使用完整 16 bit。
- byte 2:3 小端观察量：比前一量稍晚建立，从零连续增长到约 `0x46D0–0x47D0`；停止时分阶段回零。
- byte 4 bits 5:7：阶段模式约为 `5→2→0→5`，区分未建立、充电、停止仍连接和最终断开。
- byte 5 bit 0：稳态充电窗口内置位，停止开始时清除。

### 5. `0x252` 连续量组

- byte 0:1 小端观察量：建立时从零阶跃到约 `0x16D6`，保持期间缓慢变化，停止开始时回零。
- byte 2:3 小端观察量：建立时由约 `0x068F` 下降到约 `0x05C8`，停止后反向回升。

两者变化方向相反，不能继续作为一个 message-level Candidate。

## 新增的离散候选

以下点位在第一轮粗粒度排名中出现过或被相邻 byte 掩盖，但未进入第一轮最终 Hit Points；第二轮通过完整周期复核后加入：

- `0x20A byte 5 bit 4`：充电窗口相关状态。
- `0x232 byte 0 bit 6` 与 byte 1 bit 1：两个边沿一致但极性相反的充电窗口相关状态。
- `0x472 byte 4 bit 4`：建立完成后置位、停止阶段清除。
- `0x32A byte 3 bits 0:3`：非充电时为 `0xF`，充电保持时为 `0x0`。
- `0x2AA byte 0`：连接周期相关离散值，行为与 `0x2EC byte 0` 高度一致。
- `0x228`、`0x288` 的 byte 3 bits 0:1 和 byte 5 bit 7：连接周期相关字段；两帧内均另有滚动计数，第二轮只保留稳定字段。
- `0x25D byte 1 bit 7`：连接周期相关状态；byte 5 bit 0：充电建立后保持至拔枪的会话相关状态。
- `0x333 byte 1`：依次出现非充电、充电、停止仍连接和拔枪过渡值的低频阶段状态。

## 事件关系复核

| 过程 | 第二轮主要 CAN 证据 |
|---|---|
| 开小门 | `0x43D byte 5 bit 5` 在约 `40.745 s` 变化 |
| 插枪/连接 | `0x21D byte 3 bit 5`、`0x21D byte 6[4:6]`、`0x43D byte 0 bit 0` 在约 `59.55–60.05 s` 变化；`0x2EC/0x2AA/0x2E8/0x2A8/0x228/0x288/0x25D` 随后进入保持状态 |
| 充电建立 | `0x43D byte 1 bit 6`、`0x21D byte 6[4:6]` 先变化，随后 `0x264`、`0x252` 等连续量建立；多个低频窗口位在约 `174–176 s` 完成变化 |
| 稳态保持 | 离散窗口字段持续稳定；`0x132/0x252/0x264` 连续量保持在不同于非充电期的范围 |
| 正常停止 | `0x43D byte 1 bit 6` 与若干快速状态先退出，连续量随后在约 2.5 s 内衰减；仍连接字段继续保持 |
| 解锁操作 | `0x333 byte 3 bit 7` 在 E09 附近产生约 0.975 s 脉冲 |
| 拔枪/断开 | 连接字段在约 `470.17–470.57 s` 集中恢复；部分低频状态在随后约 1–5 s 完成最终回退 |

## Evidence 回指

- `0x21D` 与 `0x43D` 的核心边沿仍见第一段 ASC 第 134011–135134、390437–391116、863138–864713 行，以及第二段 ASC 第 62529–63438、74253 行。
- `0x132` 的有符号连续量跨零、稳态和停止回落代表帧见第一段 ASC 第 413416、541721、863074 行。
- `0x264` 的第一连续量建立、第二连续量进入、稳态和停止衰减代表帧见第 391075、394692、541782、863259、867973 行。
- `0x252` 的两组连续量建立与退出代表帧见第 390825、395366、863284、865306 行。
- 新增窗口候选的代表帧：`0x232` 第 391582、866039 行；`0x20A` 第 392859 行；`0x472` 第 396861、864539 行；`0x32A` 第 406283、871613 行。
- `0x228/0x288/0x2AA/0x25D` 的连接变化集中在第一段 ASC 第 135131–135929 行附近。
- 操作事件和照片锚点仍分别来自 `event_timeline.csv` 与 `session.json` 中记录的九张照片。

## 仍然不能证明的内容

1. 不能从单次实验确认任何候选的 DBC 名称、工程单位、比例、偏移或发送 ECU。
2. 不能仅凭同步边沿区分请求、许可、执行反馈和显示状态。
3. `0x132`、`0x252`、`0x264` 的连续量与外部充电过程高度相关，但不能据此命名为交流输入、直流输出或 Pack 结果。
4. 同步出现于多个 ID 的字段可能是同一状态的网关复制或不同层级反馈；本次数据无法区分。
5. `0x333 byte 3 bit 7` 只经历一次解锁操作，不能排除它代表按钮/UI 请求而非实际解锁结果。
6. `0x2D2`、`0x204` 等帧仍有更多过程相关变化，但没有足够证据精确拆分全部字段；未把边界不确定的宽泛点位继续带入最终表。

## 第二轮最终判断

第二轮保留第一轮的生命周期主结论，但收回了宽泛的 message-level Claim，也不再把明显跨 byte 的连续量拆成孤立 byte。最终 Hit Points 的数量增加，原因不是放宽标准，而是把 `0x43D` 等复合消息拆成可独立验证的字段，并补入 `0x132` 等第一轮被 byte 分析方法遮蔽的连续量。

对于诊断用途，优先级建议为：

1. `0x21D byte 6 bits 4:6` 和 `0x43D` 的连接/充电 bit：阶段识别。
2. `0x264` 的两组连续量、`0x252` 的两组连续量、`0x132 byte 2:3`：建立、保持和退出的量化观察。
3. `0x333 byte 3 bit 7`：解锁操作的辅助定位。
4. 其他重复离散字段：交叉确认，不用于单独赋予功能语义。
